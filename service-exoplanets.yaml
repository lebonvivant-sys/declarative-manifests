# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.4.0
apiVersion: v1
kind: Service
metadata:
  labels:
    app: exoplanets-app
  name: exoplanets-app
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: exoplanets-app

